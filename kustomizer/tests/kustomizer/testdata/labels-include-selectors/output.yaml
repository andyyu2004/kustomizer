---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: test-app
  labels:
    app: test-app
    version: v2
  annotations:
    description: Initial description of the test app deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: test-app
      version: v2
  template:
    metadata:
      labels:
        app: test-app
        version: v2
    spec:
      containers:
      - image: nginx
        name: app
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: test-service
  labels:
    version: v2
spec:
  selector:
    app: test-app
    version: v2
  ports:
  - port: 80
    targetPort: 80
  type: ClusterIP
