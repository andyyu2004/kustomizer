apiVersion: example.pro/v1
kind: EnvTransformer
metadata:
  name: env-transformer
  annotations:
    config.kubernetes.io/function: |
      exec:
        path: gotmpl
        args:
          - ENV=staging
          - SUBDOMAIN=staging.
          - TLD=pro
