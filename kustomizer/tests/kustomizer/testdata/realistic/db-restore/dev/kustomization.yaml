apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: partly-dev

resources:
  - ../base/
  - ./db-restore-deleter-role.yaml
  - ./db-restore-deleter-rolebinding.yaml
  - ./postgres-tailscale-service.yaml

patches:
  - path: db-restore-statefulset-patch.yaml

configMapGenerator:
  - name: finalise-script
    files:
      - finalise.sql

components:
  - ../base/components/postgres/
