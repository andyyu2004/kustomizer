apiVersion: v1
kind: Service
metadata:
  name: pgbouncer
  labels:
    app.kubernetes.io/name: pgbouncer
spec:
  ports:
    - port: 5432
      targetPort: 6432
  selector:
    app.kubernetes.io/name: pgbouncer
  type: ClusterIP
