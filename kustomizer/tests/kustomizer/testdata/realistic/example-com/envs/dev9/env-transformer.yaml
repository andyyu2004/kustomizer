apiVersion: example.com/v1
kind: EnvTransformer
metadata:
  name: env-transformer
  annotations:
    config.kubernetes.io/function: |
      exec:
        path: gotmpl
        args:
          - ENV=dev9
          - SUBDOMAIN=dev9.
          - TLD=pro
