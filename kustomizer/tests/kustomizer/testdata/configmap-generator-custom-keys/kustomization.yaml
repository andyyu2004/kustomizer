apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

generatorOptions:
  disableNameSuffixHash: true

configMapGenerator:
- name: custom-config
  files:
  - database-config=db.ini
  - nginx-conf=server.conf
  - plain-file.txt