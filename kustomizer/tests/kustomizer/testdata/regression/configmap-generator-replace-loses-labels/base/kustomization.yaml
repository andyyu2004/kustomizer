apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - configmap.yaml

labels:
  - pairs:
      base-label: from-base
      environment: development

commonAnnotations:
  base-annotation: from-base
  managed-by: kustomize-base
