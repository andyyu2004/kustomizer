apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: bob

labels:
  - pairs:
      app.kubernetes.io/instance: db-restore-staging-au

resources:
  - ./base
  - ./db-restore-cluster-rolebinding.yaml
  - ./db-restore-deleter-cluster-role.yaml

patches:
  - path: db-restore-serviceaccount-patch.yaml
