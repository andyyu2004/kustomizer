apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-deployment
spec:
  template:
    spec:
      dnsPolicy: ClusterFirst
      containers:
        - name: my-deployment
          image: my-image
      restartPolicy: Always
