apiVersion: example.com/v1alpha1
kind: MyCRD
metadata:
  name: service
spec:
  template:
    spec:
      containers:
        - name: server
          image: nginx
          command: example
          ports:
            - name: grpc
              protocol: TCP
              containerPort: 8080
