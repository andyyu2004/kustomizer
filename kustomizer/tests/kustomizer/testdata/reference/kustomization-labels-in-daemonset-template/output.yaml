apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: daemon
  labels:
    app.kubernetes.io/name: daemon
    foo: bar
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: daemon
  template:
    metadata:
      labels:
        app.kubernetes.io/name: daemon
        foo: bar
