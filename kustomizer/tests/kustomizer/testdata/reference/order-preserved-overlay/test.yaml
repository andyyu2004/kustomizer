# Source: TestOrderPreserved from baseandoverlaysmall_test.go:15
# Tests that resource order is preserved through base and overlay
# Also tests namePrefix stacking (p- + b- = p-b-) and that Namespaces don't get prefixes
