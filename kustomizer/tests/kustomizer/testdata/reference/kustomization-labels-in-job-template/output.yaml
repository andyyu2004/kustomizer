apiVersion: batch/v1
kind: Job
metadata:
  name: job
  labels:
    app.kubernetes.io/name: job
    foo: bar
spec:
  backoffLimit: 4
  template:
    metadata:
      labels:
        app.kubernetes.io/name: job
        foo: bar
    spec:
      restartPolicy: Never
